# name: CI/CD for openInterview Main Branch on Push
# on:
#   push:
#     branches: [ main ]

# jobs:
#   build-backend:
#     runs-on: ubuntu-latest
#     steps:
#       - name: Checkout Repository
#         uses: actions/checkout@v4

#       - name: Setup Docker Buildx
#         uses: docker/setup-buildx-action@v3

#       - name: Login to Docker Hub
#         uses: docker/login-action@v3
#         with:
#           username: ${{ secrets.DOCKERHUB_USERNAME }}
#           password: ${{ secrets.DOCKERHUB_TOKEN }}

#       - name: Build & Push OpenInterview
#         run: |
#           docker build \
#             --cache-from ${{ secrets.DOCKERHUB_USERNAME }}/gman-backend:Open-Interview \
#             -t ${{ secrets.DOCKERHUB_USERNAME }}/gman-backend:Open-Interview \
#             .
#           docker push ${{ secrets.DOCKERHUB_USERNAME }}/gman-backend:Open-Interview

#   deploy:
#     runs-on: ubuntu-latest
#     needs: [build-backend]
#     steps:
#       - name: Deploy to VPS
#         uses: appleboy/ssh-action@v1.0.0
#         with:
#           host: ${{ secrets.VPS_HOST }}
#           username: ${{ secrets.VPS_USER }}
#           key: ${{ secrets.VPS_SSH_KEY }}
#           script: |
#             cd /var/www/openInterview
#             echo "=== Starting Deployment ==="
            
#             # Clean Docker space (optional, be careful with prune -a)
#             # docker system prune -a -f || true
            
#             # Update git if needed
#             if [ -d .git ]; then
#               git pull --ff-only || true
#             fi

#             # Pull and deploy new containers
#             docker pull ${{ secrets.DOCKERHUB_USERNAME }}/gman-backend:Open-Interview
#             echo "✓ Pulled OpenInterview"
            
#             # Restart containers
#             # Using the production compose file
#             export DOCKERHUB_USERNAME=${{ secrets.DOCKERHUB_USERNAME }}
#             docker compose -f docker-compose.prod.yml up -d --force-recreate
            
#             echo "✓ Restarted OpenInterview"
            
#             # Wait and verify
#             sleep 10
#             echo "=== Deployment Status ==="
#             docker ps --format "table {{.Names}}\t{{.Status}}"
            
#             echo "=== IMPORTANT NEXT STEP ==="
#             echo "✅ Deployment complete on server"
