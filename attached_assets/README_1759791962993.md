# Availability — Rules Cleanup Patch
This patch:
- Adds brief instructions under each **Rules** section header.
- Removes **Exceptions** and **Troubleshoot** tabs/sections.
- Updates `availability.js` (if present) to ignore removed tabs.

## Apply (Replit)
```bash
unzip availability_rules_cleanup_patch.zip -d /tmp/oi_rules && cp -R /tmp/oi_rules/* .
node scripts/apply_rules_cleanup.mjs
# restart server (e.g., npm run dev) then:
npm i supertest cheerio
node scripts/selftest_rules_cleanup.mjs
```

## Rollback
```bash
cp -f public/availability_prev.html public/availability.html
```
