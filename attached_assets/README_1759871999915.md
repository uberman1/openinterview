# Top Menu Fix v4 — De-dup + Availability Placement

**Fixes**
- Removes duplicate menus by **replacing the first existing `<nav>`** and deleting the rest.
- On `availability.html`, the menu is **prepended** above the page title/buttons (one row at the very top).
- Still removes “New Profile” + “New Interview” CTAs.
- Response-time binder keeps HTML files untouched (guardrail-safe).

## Install on Replit
```bash
unzip top_menu_update_v4.zip -d /tmp/tmv4 && cp -R /tmp/tmv4/* .
node scripts/apply_top_menu_update_v4.mjs
# restart your server (e.g., npm run dev)
npm i supertest cheerio
node scripts/selftest_top_menu_update_v4.mjs
```
