# profiles_v2 update (Replit-ready)

This package adds **Title** and **Test file** columns to `profiles_v2.html` and serves a mock API so the table loads.

## Run
```bash
npm i
npm start
```
- Open: `https://<your-repl-url>/profiles_v2.html` (or `http://localhost:3000/profiles_v2.html` locally)

## What’s included
- `public/profiles_v2.html` with new columns and client script
- `server.js` (Express) serving:
  - `GET /api/profiles` (mock profiles)
  - static `/public` and `/test_output`
- `test_output/summary.md` and per-profile summaries to resolve links
