# Top Line Menu Update v2

**Updates requested**
1) Remove the **“New Interview”** button from the header (in addition to “New Profile”).
2) Apply the **Profiles-style top menu** to these pages: `home.html`, `uploads.html`, `subscription.html`, `password.html`, `profiles.html`, and **`availability.html`**.

**Menu**
Home · Availability · Profiles · Uploads · Subscription · Password · Log Out

## Install (Replit)
```bash
unzip top_menu_update_v2.zip -d /tmp/tmv2 && cp -R /tmp/tmv2/* .
node scripts/apply_top_menu_update_v2.mjs
# restart server (e.g., npm run dev)
npm i supertest cheerio
node scripts/selftest_top_menu_update_v2.mjs
```

This patch uses response-time injection, so your base HTML files (and their guardrail checksums) remain unchanged.
