
- Ensure playwright baseURL: http://localhost:5000
- Merge scripts into existing package.json:
  "test:e2e": "playwright test",
  "test:ui": "playwright test --ui"
- Do NOT replace your existing playwright.config.ts if it's already correct.
