# Subscriptions UI Update — Corrected (v2)
- Preserves exact HTML bytes; binder injected at response time.
- No duplicate imports: detects ESM/CJS, inserts fs/path only if missing.
- Adds /billing → /subscription.html redirect.
- Self-test validates render + binder + redirect.

## Apply
unzip subscriptions_ui_update_patch_v2.zip -d /tmp/oi_sub_v2 && cp -R /tmp/oi_sub_v2/* .
node scripts/apply_subscription_ui_update_v2.mjs
# restart server
npm i supertest cheerio
node scripts/selftest_subscription_ui_v2.mjs
