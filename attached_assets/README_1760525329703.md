# Guardrails Patch (Replit)
- Fixes avatar update + persistence
- Normalizes bottom-only links for Resumes and Attachments
- Removes duplicate Attachments sections
- Upload handlers add rows + persist (localStorage)

## Use
1) Upload/Extract
2) npm install
3) npm test
4) Open `home.html` and verify behaviors
