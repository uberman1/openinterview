# Replit Deployment Notes — Navigation Update (Availability Removed)

## Files included
- `public/js/nav-patch.js` — central nav builder (Availability removed from menu)
- `CHANGELOG.md` — change log entry
- `deploy_manifest.json` — copy map for deployment

## How to deploy
1. Unzip into your project root.
2. Overwrite `public/js/nav-patch.js`.
3. Reload the app (or restart the Replit server).

## QA checklist (5–10 minutes)
- Home (`/home.html`): 4 items show — Home, Subscription, Password, Log Out.
- Subscription (`/subscription`): 4 items; Subscription is active.
- Password (`/password`): 4 items; Password is active.
- Profile Editors (`/profile_edit_enhanced.html`, `/profile_edit.html`): nav shows 4 items.
- Direct link `/availability`: page loads; nav still shows 4 items (no self-link).
- Console: no errors.
- Mobile view: spacing/wrapping OK.

## Rollback
Open `public/js/nav-patch.js` and uncomment the line:
```js
// nav.appendChild(mk('Availability', '/availability', isAvail));
```

## Notes
- No other UI components were modified.
- Availability page remains accessible via direct URL or bookmarks.
