# Module 13A — Login UI (Email/Password) — No new deps

Goal
- Make the /login screen fully functional: email + password inputs, submit to /api/v1/auth/login,
  save token in localStorage, and redirect to /dashboard (or /admin for admin@example.com).

Apply
1) Replace client/src/pages/Login.jsx with this archive's version.
2) Add client/src/lib/api.js and client/src/lib/auth.js.
3) Ensure route exists: <Route path="/login" element={<Login/>} />
4) Build + run: npm run build && npm run dev
