# Cutover Checklist (tick each item)

[ ] Add/overwrite all Module‑1 files in the repo, preserving paths.
[ ] Set run command to `npm start` (node index.js).
[ ] Start app; confirm `/` redirects to `/login.html`.
[ ] Open `/login.html`; verify pixel-identical UI.
[ ] Try `/#/login`; confirm it redirects to `/login.html`.
[ ] Use demo: `user@example.com` → routes to `/profile.html` (may 404 until Module 2).
[ ] Use demo: `admin@example.com` → routes to `/admin.html` (may 404 until Module 4).
[ ] Run `npm test`; all guardrail and API tests pass.
[ ] No new dependencies added.
[ ] Commit with message: "Module‑1 Login cutover: static login + guardrails enabled".