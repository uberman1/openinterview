# DIFFS — Module 4 Apply Instructions

1) Add these new files:
- /server/auth.real.js
- /server/auth.routes.js

2) Replace these files:
- server.server.patch.js → /server/server.js
- scripts.selftest.m04.js → /scripts/selftest.js

3) Build and test Module 04:
```bash
npm run build
MODULE_NUM=04 node scripts/selftest.js
```

Notes
- USE_MOCK_AUTH=true will mount the mock auth; otherwise the real adapter is used.
